
= Regenerate certificates and keystores

Run following script `generate_certs.sh` and copy all files from the execution folder (not from `tmp`) into `src/main/resources`.

Content of the script file:

[source,bash]
----
include::./generate_certs.sh[]